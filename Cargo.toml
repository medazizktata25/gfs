[workspace]
resolver = "2"
members = [
    "crates/adapters/compute-docker",
    "crates/adapters/db-providers",
    "crates/adapters/compute-kubernetes",
    "crates/adapters/storage-apfs",
    "crates/adapters/storage-btrfs",
    "crates/adapters/storage-file",
    "crates/adapters/storage-kubernetes",
    "crates/applications/cli",
    "crates/applications/console",
    "crates/applications/mcp",
    "crates/applications/proxy",
    "crates/applications/website",
    "crates/config",
    "crates/domain",
    "crates/telemetry",
]
# The gfs Table Access Method extension is a pgrx cdylib with its own
# panic=unwind profile and pinned toolchain; build it with cargo-pgrx from its
# own directory (`cd crates/extensions/gfs && cargo pgrx ...`). Excluded so a
# plain `cargo build` of the workspace never pulls in pgrx.
exclude = ["crates/extensions/gfs"]

[workspace.package]
version      = "0.4.0"
edition      = "2024"
authors      = ["Guepard Corp <team@guepard.run>"]
license      = "MIT"
repository   = "https://github.com/Guepard-Corp/gfs"
rust-version = "1.93"

[workspace.dependencies]
rocket       = { version = "0.5", features = ["json"] }
tokio        = { version = "1", features = ["full"] }
tokio-stream = { version = "0.1" }
tonic        = { version = "0.12" }
tonic-build          = { version = "0.12" }
protoc-bin-vendored  = { version = "3" }
prost        = { version = "0.13" }
sysinfo      = { version = "0.32" }
clap         = { version = "4", features = ["derive", "env"] }
anyhow               = { version = "1" }
async-trait           = { version = "0.1" }
thiserror            = { version = "2" }
chrono               = { version = "0.4", features = ["serde"] }
bincode              = { version = "2" }
reqwest              = { version = "0.12", features = ["json"] }
serde                = { version = "1", features = ["derive"] }
serde_json           = { version = "1" }
tempfile             = { version = "3" }
toml                 = { version = "0.8" }
tracing              = { version = "0.1" }
tracing-subscriber   = { version = "0.3", features = ["env-filter", "json"] }
utoipa               = { version = "5.4.0", features = ["rocket_extras"] }
utoipa-swagger-ui    = { version = "8", features = ["rocket"] }
kube                 = { version = "3.0.1", features = ["runtime", "derive", "ws"] }
k8s-openapi          = { version = "0.27.0", features = ["latest"] }
bollard              = { version = "0.20" }
# Bundled SQLite: compiles the amalgamation in, so the SQLite version is pinned by
# Cargo.lock rather than by whatever `sqlite3` happens to be on the host.
rusqlite             = { version = "0.40", features = ["bundled"] }
sha2                 = { version = "0.10" }
tar                  = { version = "0.4" }
rmcp = { version = "0.16.0", features = [
    "server",
    "transport-streamable-http-server",
    "transport-io",
] }
posthog-rs             = { version = "0.4", default-features = false }
uuid                   = { version = "1", features = ["v4"] }
gfs-domain                 = { path = "crates/domain" }
gfs-telemetry              = { path = "crates/telemetry" }
gfs-config                 = { path = "crates/config" }
gfs-storage-apfs           = { path = "crates/adapters/storage-apfs" }
gfs-storage-btrfs          = { path = "crates/adapters/storage-btrfs" }
gfs-storage-file           = { path = "crates/adapters/storage-file" }
gfs-storage-kubernetes     = { path = "crates/adapters/storage-kubernetes" }
gfs-compute-docker         = { path = "crates/adapters/compute-docker" }
gfs-db-providers           = { path = "crates/adapters/db-providers" }
gfs-compute-kubernetes     = { path = "crates/adapters/compute-kubernetes" }
gfs-cli                    = { path = "crates/applications/cli" }
gfs-mcp                    = { path = "crates/applications/mcp" }

[profile.release]
opt-level     = 3
lto           = "thin"
codegen-units = 1
strip         = true
